let numero;
let soma = 0;

do {
  numero = parseInt(prompt("Digite um número inteiro (0 para sair da sequência!)"));
  soma += numero;
} while (numero != 0);

console.log("A soma dos números digitados é: " + soma);
